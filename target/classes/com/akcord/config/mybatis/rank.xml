<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.akcord.main.rank.dao.RankDao">
    
    <select id="getQna">
        select rownum, subject, qna_id 
        from
        (select subject, hit, qna_id
        from qna
        order by hit)
        where rownum &lt;= 10
    </select>
    
    <select id="getQnaComment">
        select rownum, subject
        from
        (select subject, hit
        from qna
        order by hit)
        where rownum &lt;= 10
    </select>

    <select id="getGroup">
        select group_id, count(user_id) hit, group_name
        from
            (select group_id, group_name from grouproom_list)
        order by hit
    </select>

    <select id="getMyroom">
        select subject, hit, myroom_id
        from myroom_next
        where is_share = 1
        order by hit
    </select>
    
</mapper>

