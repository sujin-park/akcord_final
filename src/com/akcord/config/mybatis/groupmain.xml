<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper     
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.akcord.group.dao.GroupMainDao">

    <select id="gMemberlist" parameterType="String" resultType="groupListDto">
        select g.group_id groupId, u.name name, u.id userId, m.major_name majorName, gl.reg_date regDate
                from grouproom_list gl, grouproom g, users u, major m
                where gl.group_id = g.group_id
                and u.user_id = gl.user_id
                and m.major_id = u.major_id
                and g.group_id = 2
                and gl.user_id != 1
    </select>
    
    
</mapper>